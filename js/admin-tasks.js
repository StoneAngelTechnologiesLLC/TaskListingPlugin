//admin-jobs.js

// Adding calanders to fields, to allow picking date, with a click of the mouse.
jQuery(document).ready(function()
{
			//class to target, function to preform.
	jQuery('.datepicker').datepicker();

});